name: Deploy pythonanywhere webapp

on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Upload files
      # ...

      - name: Reload webapp
        uses: jensvog/pythonanywhere-webapp-reload-action@v1
        with:
          host: 'www.pythonanywhere.com'
          username: {{ secrets.Mrutyunjaya07 }}
          api-token: {{ secrets.c5842bc9757aa3e0479b92fe79918b9dd73ee2cc }}
          domain-name: {{ https://mrutyunjaya07.pythonanywhere.com/ }}